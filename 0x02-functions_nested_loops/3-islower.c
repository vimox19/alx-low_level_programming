#include "main.h"

/**
 * _islower - Verifies if a character is in lowercase form.
 * @c: The character to be examined.
 *
 * Returns: 1 if the character is in lowercase, 0 otherwise.
 */
int _islower(int c)
{
    return (c >= 'a' && c <= 'z');
}
